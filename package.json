{
  "name": "natsuume_web",
  "version": "1.0.0",
  "description": "ホームページ作るzoi",
  "scripts": {
    "start": "run-p start:server start:client",
    "start:server": "docker build -t natsuumeweb . && docker run -p 8080:8080 -it natsuumeweb",
    "start:client": "cd client/ && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "env-cmd": "^10.1.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
