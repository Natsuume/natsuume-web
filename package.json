{
  "name": "natsuume_web",
  "version": "1.0.0",
  "description": "ホームページ作るzoi",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "env-cmd": "^10.1.0"
  }
}
